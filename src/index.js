import React from 'react';
import ReactDOM from 'react-dom/client';
import LuxuryRefillFinder from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <LuxuryRefillFinder />
  </React.StrictMode>
);
